# ============================================
# REQUIRED: API Keys
# ============================================

# OpenRouter API Key
# Get yours at: https://openrouter.ai/keys
# Used for: AI model inference (Google Gemini 2.5 Flash)
# Implementation: Server-side API calls to OpenRouter's chat completions endpoint
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Cloudflare Turnstile Keys
# Get yours at: https://dash.cloudflare.com/?to=/:account/turnstile
# Used for: Bot protection and abuse prevention
# Implementation:
#   - VITE_TURNSTILE_SITE_KEY: Client-side widget rendering (VITE_ prefix exposes to browser)
#   - TURNSTILE_SECRET_KEY: Server-side token verification
VITE_TURNSTILE_SITE_KEY=your_turnstile_site_key
TURNSTILE_SECRET_KEY=your_turnstile_secret_key

# ============================================
# OPTIONAL: Server Configuration
# ============================================

# Server port (defaults to 3001 if not set)
PORT=3001

# Environment mode (production | development)
# Affects CORS, static file serving, and error messages
NODE_ENV=production

# Git commit hash for version tracking (optional)
# Automatically set in CI/CD, shows in /health endpoint
GIT_COMMIT_HASH=
